# Visual integration audit

Updated 2026-09-08. The original audit below records the missing connection; implementation has now replaced that baseline. The complete image backfill remains open.

## Implemented connection

### Complete topology correction

The initial integration connected media storage and the player but lowered scenario graphs to their input/event/outcome boundary. That was inadequate: the frozen records already contained operations, mechanic bindings, native cells and routes, and selected SQL blueprint definitions contained edges absent from the normalized website edge query. The primary website circuit now consumes these complete sources.

The new Python compiler produces 1,602 diagrams: all 35 selected SQL blueprints, 694 expanded operation views, 117 native execution views and 756 mechanic expression dependency views. Together they render 41,803 component occurrences and 41,331 relation occurrences, with zero source components/routes omitted within their declared profiles. These are view occurrences, not unique estate entities. The speech provider view alone preserves 609 native cells and 690 routes. The largest mechanic dependency graph contains 1,493 components.

The compiler preserves explicit branch, fan-out, convergence, terminal, return, recurrence and cancellation semantics. Expression edges describe named dependencies, not inferred runtime control. Three source blueprint endpoint references lack node declarations; their unresolved shapes and findings remain visible. The existing 14 unmatched scenario entries and changed capsule lineage are not silently rebound.

All graph data, source SVGs, material masks, catalog entries, coverage receipts and versioned runtime files are retained in SQL bundles. The full blueprint/operation workbench is primary on capability pages and the homepage. Reviewed films/editions and compact boundary contracts have separate surfaces. Browser validation checks complete material and base rendering, readable large-graph zoom, component search/inspection and complete flow through all fan-out members and terminal branches. Publication validation independently compares delivered SVG node/route identities to the graph and rejects missing endpoints, missing materials or incomplete catalogs.

The current delivery contains 1,578 diagrams through 810 scenario entries across 217 capabilities, including all 35 SQL blueprints. The remaining 24 compiled mechanic dependency diagrams belong to the capability whose frozen scenario identities cannot bind to current definitions; their original bundles remain in SQL. An independent union check verifies all 4,992 native source cells and 5,498 source routes, including the additional `write-binary-artifact` cells outside scenario assignments. Every one of the 30,164 compiled mechanic expression occurrences resolves to its exact mechanic definition and responsibility.

Trace Flow now traverses every declared route and component to a visible completion state, including alternative branches, convergence, return/recurrence, provider references and disconnected components. Forks animate simultaneous silver-ball traces, including operation/scenario-call forks. Independent mechanic operands animate together; required convergence and dependency joins wait for their incoming traces. Each cyclic route is visited once. Pause/resume, replay, speed and camera-follow controls are implemented. Browser checks completed 10/10 blueprint routes and 690/690 speech-provider routes, covering 9/9 and 609/609 components respectively. These are diagram walkthroughs, not provider execution receipts.

The production build passes for 516 pages. Six Python tests cover source preservation and native cells without scenario assignments; six player tests include finite, complete tracing over all 1,602 compiled diagrams. SQL-only reconstruction verifies all 3,591 delivered artifacts (209,825,481 bytes), including the selected runtime overlay, against media manifest SHA-256 `ae5a356d26aea6cfe279a1a7b775c381aa31e641efd5c157b4aa7f6620956e21`.

### Earlier media connection

- SQL migrations `005-media-registry` and `006-media-integrity` are installed. Original bytes, derivatives, generation requests, exact semantic foreign keys, reviews, selections and circuit bundles are persisted. Direct cross-entity writes and edits to immutable records are rejected by SQL.
- All 1,393 current capability/scenario/mechanic/provider definitions have image requirements, including definitions without a public page. Ten distinct subjects have reviewed images: two imported capability illustrations, four new Nano Banana mechanic images and four new Nano Banana scenario images. Their 20 card/detail selections are published. Another 1,383 generation jobs remain queued; the full paid backfill has not run.
- The lab's 15 reviewed component materials, original generation receipts and two complete visual editions are stored. SCL compilation produced 810 current, identity-matched scenario views across 217 capabilities. Their 5,568 input/output files are stored in SQL. Twelve frozen scenario-line identities could not bind to the current model; `resolve-platform-responsibilities` has changed capsule lineage. Those gaps are retained.
- The website renders actual artwork, scenario images, films/captions, canonical material/base circuit views and the existing authored silver-ball player. New SCL boundary projections have no authored trace; they do not invent playback. The estate boundary renderer remains the readable fallback for unmatched scope.
- Warm-paper editorial CSS, image compositions, semantic workbench colors, responsive layouts and source-based featured selection replace the former shell. Brand copy is centralized; entity content remains identity-bound.
- Publication validation pins the media manifest and verifies delivered file hashes and semantic ownership. Regression tests reject altered media, cross-entity artwork and cross-capability circuits. SQL-only reconstruction and deployment are recorded below when verified.

See [media operations](media-operations.md) for the running pipeline and recovery commands. A successful staging build does not close complete-artwork, authoring, download or other P1 gates.

### Earlier verification (before the topology correction)

The production build passes for 516 pages. The 41-test suite includes media tampering, exact entity/definition ownership and circuit-owner regression tests. Ten SQL rejection/readback checks plus all ten selected original-image round trips pass. SQL-only reconstruction verifies all 2,336 publication artifacts, totaling 96,297,260 bytes, against media manifest SHA-256 `9034ff1ad9b5b58d67407d91f5c1ae0407ccf2d21b7dfdbc83ee66216c627a04`. Desktop and 375-pixel layout checks show no page overflow; the existing silver-ball player reaches its authored endpoint while retaining the GAP evidence label.

## Existing assets and pages

The content lab is `C:\lab\repos\content-creation-mission`. The following inventory counts specific directories; it is not an estate-wide count of unique subjects or deduplicated images.

| Location | Observed artifacts | Meaning |
| --- | --- | --- |
| `docs/visual-assets/` | 9 PNG references | Capability/provider nodes, junction, fulfillment/failure terminals, visual alphabet, symbol atlas, circuit and motion design references |
| `outputs/generated/` | 33 JPG files and 33 `GENERATED` receipts | Receipts name `gemini-3-pro-image`; generation is not by itself editorial approval or a current database entity binding |
| `outputs/component-enhancements/` | 15 selected, reviewed component materials; extra attempts and shape guides also retained | All 15 selected file hashes match both their generation receipts and `evaluations/component-enhancement-review.json`; approved for canonical-mask compositing |
| `samples/infographic-grammar/` | 65 SVG files, 25 PNG files, 2 MP4 files, plus contracts and viewer assets | Includes base/enhanced infographic products, component exports and motion; these file counts include alternate representations of the same subjects |
| `declarations/capability-content/` | 219 content contracts | 2 `EDITORIALLY_REVIEWED`, 217 `NEEDS_DIRECTION` |
| `declarations/capability-pages/` | 2 composed page declarations | Interlock Agent Operation and Generate Governed Narration |
| `samples/scl/` and `evaluations/scl-coverage.json` | Frozen source reveal covering 219 capabilities and 823 scenarios | Coverage report records 4,992 native cells and 5,498 native edges; this is source reveal coverage, not complete artwork coverage or approval of every narrative |

The visual references are present. For example:

- [Visual alphabet](C:/lab/repos/content-creation-mission/docs/visual-assets/sidefx-visual-alphabet-enhanced.png)
- [Junction](C:/lab/repos/content-creation-mission/docs/visual-assets/junction-node.png)
- [Fulfillment terminal](C:/lab/repos/content-creation-mission/docs/visual-assets/fulfillment-terminal.png)
- [Failure terminal](C:/lab/repos/content-creation-mission/docs/visual-assets/failure-terminal.png)
- [Capability page system](C:/lab/repos/content-creation-mission/docs/CAPABILITY-VISUAL-PRODUCT-SYSTEM.md)
- [Infographic grammar](C:/lab/repos/content-creation-mission/docs/SIDEFX-INFOGRAPHIC-GRAMMAR.md)
- [Silver-ball circuit player](C:/lab/repos/content-creation-mission/docs/CIRCUIT-FLOW.md)

The existing local preview servers were running during inspection:

- [Interlock capability edition](http://127.0.0.1:8765/samples/capability-pages/interlock-agent-operation/index.html)
- [Narration capability edition](http://127.0.0.1:8765/samples/capability-pages/generate-governed-narration/index.html)
- [Agentic Engineering page](http://127.0.0.1:8765/samples/agentic-engineering/index.html)
- [Infographic studio](http://127.0.0.1:8765/samples/infographic-grammar/index.html)
- [SCL workbench](http://127.0.0.1:8766/samples/scl/index.html)

The two inspected page designs use warm paper reading surfaces, precise dark typography, generous spacing, fine rules, composed media and dark circuit workbenches. The infographic system supplies luminous materials inside canonical shapes. The new website composition follows these references.

## Baseline before implementation

| Layer | Evidence | Consequence |
| --- | --- | --- |
| SQL persistence | Existing database `probe()` reports 125 user tables in `analysis`, `model`, `source`; no media schema or media/visual/asset/image tables | The proposed media relations in the database design have not been installed. The existing `source.content_object` byte store represents captured source, not an implemented generated-image registry |
| Generator integration | Content-lab `generate_gemini.py` and `generate_component_assets.py` write local files and receipts | Successful local generation has no database ingestion/selection step connected to this website |
| Website publication | `scripts/publish-estate.mjs`, function `visualRequirements`, explicitly says no media service is connected and writes `REQUIRED` with null revision, digest and URL | Every published image slot is empty regardless of existing local artwork |
| Coverage | `generated/estate-publication.json`: 218 published capabilities, 191 mechanics, 74 providers, 824 scenario faces, 1,184 required visual slots, 0 ready | The slot count is 218 × 3 + 191 × 2 + 74 × 2. It excludes scenario artwork and the managed capability that has no published page |
| Scenario contract | `contracts/estate.ts`, `ScenarioFace` has no visual collection | A scenario cannot currently carry its own selected image into rendering |
| Circuit integration | Website schema supports six node primitives and three route families; content lab has fifteen canonical primitives and seven route families | The website is a limited independent renderer, not integration of the full existing grammar, materials and player |
| CSS semantics | `components/circuit/scl-theme.ts` maps `OUTCOME` to the shell's violet `projection` token and `PROVIDER_SLOT` to amber `telemetry` | Explicit mappings still differ from the lab's emerald outcome and sapphire provider-port materials; semantic styling needs the actual pinned grammar |
| Asset delivery | The website `public/` folder has no artwork; no database media reader is connected to pages | Adding a few CSS rules cannot make the missing assets appear |
| Release checks | Container/publication checks can pass with `visualsReady: 0` | Successful staging deployment proves packaging and existing behavior, not the specification's completed visual experience |

The live database inventory was re-read through the restricted query runner with no truncation. It reports 219 managed capabilities, 289 total capability identities across 304 definitions, 191 mechanics, 74 providers, 824 scenarios and 35 blueprints. Query evidence is retained locally at `C:\lab\sidefx-database\data\website-visuals\visual-audit-live-2026-09-08.json`.

That query pins snapshot `sha256:1a770ac0795d10665a88166f8d8c968dc5b2d030fdfab2b837aa6071d135f9e9` and mapping `sha256:e036610730972f246d8e33cd882e45dd793013076aea2e401086b93d4993a7ee`. The lab's frozen reveal and the committed website publication use their own source identities. Import must reconcile exact subject ownership and definitions; matching slugs or similar counts is insufficient. The lab's 823-scenario reveal must not silently replace the database's 824-scenario selection. Likewise, native source edges cannot simply be asserted to be normalized SQL blueprint edges.

## Clarified requirements

The website specification now makes every scenario's own Nano Banana image mandatory. The previous “when featured” qualification was weaker than the user's requirement. Capability, scenario, mechanic and provider artwork must all be retained as original bytes and derivatives with exact database subject/revision bindings. Shared component materials remain reusable but do not satisfy a dedicated entity image.

The mandatory scope is 1,308 identities for managed capabilities plus scenarios/mechanics/providers, or 1,378 including all selected platform capability identities. This is a coverage denominator, not a claim that all artwork has already been created or an estimate of paid calls. Each selected definition needs a valid binding and every generated candidate needs its recorded review state.

The database must also retain complete infographic/playback bundles: authored SCL, canonical graph, base/enhanced SVG, layout anchors, animation plans, referenced component materials and versioned player assets. Reconstructing a published circuit must not depend on a local Windows path. Posters, captions and film bytes are retained where included in that published product.

CSS acceptance in spec §6.7 now requires typed entity/media variants, canonical semantic styles, deliberate image composition and crops, editorial layout, complete interaction states, and visual comparisons to the finished content-lab pages at the specified responsive widths. Build success does not establish visual acceptance.

## Original implementation checklist

1. Implement the additive database media registry, byte ingestion/retrieval, exact definition bindings, review/selection and requirement coverage. Prove actual-image round trips and complete circuit reconstruction.
2. Inventory/import the existing verified materials and reviewed products with their provenance, exact source bindings and artifact closure. Preserve reusable global grammar assets separately from dedicated entity artwork. Stale or unmatched products remain explicitly unresolved.
3. Connect the existing Nano Banana preparation/generation/review workflow to durable jobs and database ingestion. Author subject-specific direction and generate missing capability/scenario/mechanic/provider artwork in bounded, resumable batches. A provider response is incomplete until its bytes and lineage are committed.
4. Integrate the versioned content-lab grammar, materials and flow player; implement the composed capability/scenario/catalog/inspector surfaces using the visual contract and reference designs. Publish approved media metadata and rebuildable delivery copies from SQL.
5. Validate estate-wide coverage, image fidelity, CSS/reference comparisons, accessible interaction, source-preserving playback and media delivery in the final container before claiming the visual launch gates are met.

This historical checklist is superseded by the implemented connection above. Complete estate artwork remains a separate unfinished production gate.
