node_modules/
.next/
out/
.env*.local
*.tsbuildinfo
next-env.d.ts
.env
.env.*
!.env.example
artifacts/

# The media publication is generated content that SQL retains in full and can rebuild byte for
# byte. It is restored before a build (`npm run restore:media`), never committed.
public/media/
